package de.hbt.power.client;
/*
import de.hbt.power.model.Skill;
import de.hbt.power.repo.SkillCategoryRepository;
import de.hbt.power.repo.SkillRepository;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.actuate.metrics.CounterService;
import org.springframework.boot.test.autoconfigure.web.client.RestClientTest;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.boot.web.client.RestTemplateBuilder;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.test.context.junit4.SpringRunner;
import org.springframework.web.client.RestTemplate;

import static org.junit.Assert.*;

@RunWith(SpringRunner.class)
@SpringBootTest
@ActiveProfiles("test")
public class ItsYourSkillsServiceTest {

    @Autowired
    private RestTemplate restTemplate;

    @Test
    public void getAlternativeSkillTaxonomy() throws Exception {
        ItsYourSkillsService itsYourSkillsService = new ItsYourSkillsService(restTemplate, null);
        Skill s = new Skill();
        s.setQualifier("Java");
        String res = itsYourSkillsService.getAlternativeSkillTaxonomy(s);
        assertNotNull(res);
    }

}*/